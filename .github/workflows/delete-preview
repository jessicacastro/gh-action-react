name: Remove the preview on Vercel
on:
  pull_request:
  types: [closed, deleted]
jobs:
  preview-delete:
    environment: preview
    runs-on: ubuntu-latest
    steps:
      - uses: snaplet/vercel-action@v3
        with:
          delete: true
