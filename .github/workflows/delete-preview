name: Delete preview on Vercel
on:
  pull_request:
  delete:
    environment: preview
    runs-on: ubuntu-latest
    steps:
      - uses: snaplet/vercel-action@v3
        with:
          delete: true
