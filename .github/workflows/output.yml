name: Output information
on: workflow_dispatch
jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Output Github Context
        run: echo "${{ toJson(github) }}"
      - name: Output Github context variables
        run: echo "${{ toJson(github.env) }}"
